//dependencies

var express = require("express");
var bodyParser = require("body-parser")
var mongoose = require("mongoose");

//these dependencies make it possible to scrape

var request = ("request");
var cheerio = ("cheerio");

//initialize express
var app express();

var databaseUrl = "scraper";
var 